export const privilegeColors: { [key: string]: string } = {
  M: "#c76961", // Manager (default color when highlighted)
  P: "#f5d654", // Property (default color when highlighted)
  L: "#2bccc4", // Leasing (default color when highlighted)
  A: "#2790ac", // Admin (default color when highlighted)
  default: "#dddddd", // Default color for non-highlighted privileges
};
